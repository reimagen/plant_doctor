# Server-only API key for Gemini Content API (care guides, rescue plans)
# DO NOT prefix with NEXT_PUBLIC_ - this stays on the server
GEMINI_API_KEY=

# Client-side API key for Gemini Live API (real-time audio/video)
# MUST be domain-restricted in Google Cloud Console
NEXT_PUBLIC_GEMINI_API_KEY=
